-r requirements.txt

ruff>=0.6.0
pytest>=8.0.0
pytest-cov>=5.0.0

# Interfaz visual de la API. Empaqueta ~9 MB de activos de Swagger UI, asi que
# no entra en la imagen de produccion: el contrato en docs/openapi.yaml se sirve
# igual sin esta dependencia.
flasgger>=0.9.7
pyyaml>=6.0

# Fase A — regenerar notebooks/energiai.ipynb y scripts/generar_dataset.py.
# joblib, pandas y scikit-learn ya vienen de requirements.txt (son de
# producción también, no solo del notebook — ver el porqué ahí). Acá solo lo
# que es EXCLUSIVO de re-entrenar o auditar el notebook.
numpy>=1.26.0
matplotlib>=3.8.0
jupyter>=1.0.0
nbformat>=5.10.0
nbclient>=0.10.0
