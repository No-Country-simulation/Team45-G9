-r requirements.txt

ruff>=0.6.0
pytest>=8.0.0
pytest-cov>=5.0.0

# Interfaz visual de la API. Empaqueta ~9 MB de activos de Swagger UI, asi que
# no entra en la imagen de produccion: el contrato en docs/openapi.yaml se sirve
# igual sin esta dependencia.
flasgger>=0.9.7
pyyaml>=6.0

# Fase A — regenerar notebooks/energiai.ipynb y scripts/generar_dataset.py.
# No hacen falta en producción (el modelo ya viene serializado en modelos/),
# solo para quien vaya a re-entrenar o auditar el notebook.
scikit-learn>=1.4.0
pandas>=2.2.0
numpy>=1.26.0
matplotlib>=3.8.0
joblib>=1.3.0
jupyter>=1.0.0
nbformat>=5.10.0
nbclient>=0.10.0
